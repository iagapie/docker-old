# Frontend for docker

## Credentials
**user:** `root`
**pass:** `root`

## Install Steps:
    # cd <project-dir>
	# docker network create traefik
	# docker-compose up -d
	# ./frontend/bin/exec bash
	# composer install
	# yarn install
	# yarn encore [dev|prod]
	
